// ── WPF temporary projects must resolve production filesystem evidence types explicitly ──
global using System.IO;

// ── Retry classification must resolve HttpRequestException without implicit-using assumptions ──
global using System.Net.Http;
