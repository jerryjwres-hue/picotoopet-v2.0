PicotooPet Interactive Pet v1 local-only assets.

These mascot files are embedded into the existing WPF application. The UI may animate, swap, drag, tilt, and react to pointer input, but it never writes task, approval, Worker, Core, provider, or persistence state.
