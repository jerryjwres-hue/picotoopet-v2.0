<UserControl x:Class="PicotooPet.Desktop.Controls.PetMascot.PetMascotControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             MinWidth="312"
             MinHeight="322"
             Focusable="False"
             Background="Transparent"
             Loaded="OnLoaded"
             Unloaded="OnUnloaded"
             MouseEnter="OnMouseEnter"
             MouseMove="OnMouseMove"
             MouseLeave="OnMouseLeave"
             MouseLeftButtonUp="OnMouseLeftButtonUp">
  <UserControl.Resources>
    <Style x:Key="MascotActionButtonStyle" TargetType="Button">
      <Setter Property="Foreground" Value="#FF1D4ED8" />
      <Setter Property="Background" Value="#FFF8FBFF" />
      <Setter Property="BorderBrush" Value="#FFD9E7FF" />
      <Setter Property="BorderThickness" Value="1" />
      <Setter Property="FontSize" Value="13" />
      <Setter Property="FontWeight" Value="SemiBold" />
      <Setter Property="Padding" Value="12,7" />
      <Setter Property="Margin" Value="0,0,8,0" />
      <Setter Property="Cursor" Value="Hand" />
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="Button">
            <Border x:Name="ButtonBorder"
                    Background="{TemplateBinding Background}"
                    BorderBrush="{TemplateBinding BorderBrush}"
                    BorderThickness="{TemplateBinding BorderThickness}"
                    CornerRadius="11"
                    Padding="{TemplateBinding Padding}">
              <ContentPresenter HorizontalAlignment="Center"
                                VerticalAlignment="Center" />
            </Border>
            <ControlTemplate.Triggers>
              <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="ButtonBorder" Property="Background" Value="#FFEEF5FF" />
                <Setter TargetName="ButtonBorder" Property="BorderBrush" Value="#FFB9D2FF" />
              </Trigger>
              <Trigger Property="IsPressed" Value="True">
                <Setter TargetName="ButtonBorder" Property="Opacity" Value="0.82" />
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>
  </UserControl.Resources>

  <Grid x:Name="RootGrid" Background="Transparent">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
    </Grid.RowDefinitions>

    <Border x:Name="CalloutBorder"
            Grid.Row="0"
            MaxWidth="296"
            Margin="8,0,8,6"
            Padding="15,13"
            HorizontalAlignment="Left"
            Background="#FCFFFFFF"
            BorderBrush="#FFE3EAF4"
            BorderThickness="1"
            CornerRadius="18"
            Visibility="Collapsed"
            Panel.ZIndex="4">
      <Border.Effect>
        <DropShadowEffect BlurRadius="20"
                          ShadowDepth="4"
                          Opacity="0.12"
                          Color="#FF334155" />
      </Border.Effect>
      <StackPanel>
        <TextBlock Text="茅台"
                   FontSize="12"
                   FontWeight="SemiBold"
                   Foreground="#FF64748B" />
        <TextBlock x:Name="CalloutText"
                   Margin="0,4,0,10"
                   FontSize="14"
                   Foreground="#FF172033"
                   TextWrapping="Wrap" />
        <StackPanel x:Name="CalloutActions"
                    Orientation="Horizontal">
          <Button Content="新建任务"
                  Style="{StaticResource MascotActionButtonStyle}"
                  Click="OnNewTaskClick" />
          <Button Content="看看进度"
                  Style="{StaticResource MascotActionButtonStyle}"
                  Margin="0"
                  Click="OnProgressClick" />
        </StackPanel>
      </StackPanel>
    </Border>

    <Grid x:Name="MascotStage"
          Grid.Row="1"
          MinHeight="270"
          ClipToBounds="False">
      <Ellipse Width="176"
               Height="26"
               Margin="0,0,0,14"
               HorizontalAlignment="Center"
               VerticalAlignment="Bottom"
               Fill="#120F172A"
               IsHitTestVisible="False">
        <Ellipse.Effect>
          <BlurEffect Radius="8" />
        </Ellipse.Effect>
      </Ellipse>

      <Image x:Name="MascotImage"
             Width="272"
             Height="272"
             HorizontalAlignment="Center"
             VerticalAlignment="Bottom"
             Stretch="Uniform"
             SnapsToDevicePixels="True"
             UseLayoutRounding="True"
             RenderTransformOrigin="0.5,0.62">
        <Image.RenderTransform>
          <TransformGroup>
            <ScaleTransform x:Name="HoverScale" ScaleX="1" ScaleY="1" />
            <ScaleTransform x:Name="ClickScale" ScaleX="1" ScaleY="1" />
            <RotateTransform x:Name="HoverRotate" Angle="0" />
            <TranslateTransform x:Name="BreathTranslate" Y="0" />
            <TranslateTransform x:Name="PointerTranslate" X="0" Y="0" />
          </TransformGroup>
        </Image.RenderTransform>
      </Image>
    </Grid>
  </Grid>
</UserControl>
